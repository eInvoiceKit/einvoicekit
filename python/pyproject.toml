[build-system]
requires = ["hatchling >= 1.26"]
build-backend = "hatchling.build"

[project]
name = "einvoicekit"
version = "0.2.0"
description = "Validate any EN 16931 e-invoice (Factur-X, ZUGFeRD, XRechnung, UBL, CII) against the full official rule set. One command, no key needed to start."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "Vizalgo SAS" }]
keywords = [
  "factur-x",
  "facturx",
  "zugferd",
  "xrechnung",
  "en16931",
  "e-invoice",
  "einvoice",
  "ubl",
  "cii",
  "peppol",
  "validation",
  "validator",
]
classifiers = [
  "Development Status :: 4 - Beta",
  "Environment :: Console",
  "Intended Audience :: Developers",
  "Operating System :: OS Independent",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Office/Business :: Financial :: Accounting",
  "Topic :: Software Development :: Libraries",
  "Typing :: Typed",
]
dependencies = []

[project.urls]
Homepage = "https://einvoicekit.com/docs/"
Repository = "https://github.com/eInvoiceKit/einvoicekit"
Issues = "https://github.com/eInvoiceKit/einvoicekit/issues"

# Package, import name and command are all `einvoicekit`. The package never
# installs a `facturx` module: the `factur-x` distribution already owns that
# path, and two distributions writing the same path corrupt each other's install.
[project.scripts]
einvoicekit = "einvoicekit.cli:run"

[dependency-groups]
dev = ["pytest >= 8", "ruff >= 0.12", "build >= 1.2"]

[tool.hatch.build.targets.wheel]
packages = ["src/einvoicekit"]

[tool.hatch.build.targets.sdist]
include = ["src/einvoicekit", "tests", "README.md", "LICENSE", "pyproject.toml"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
